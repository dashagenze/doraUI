1. выбираю режим выбора single/multiple
2. выбираю опции/опцию
3  single - состояние объект, multiple - массив объектов
3. записываю их значение в пропс через пропс setState
4. отображаю пропс в компоненте выше
